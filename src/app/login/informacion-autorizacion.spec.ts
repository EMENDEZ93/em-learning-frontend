import { InformacionAutorizacion } from './informacion-autorizacion';

describe('InformacionAutorizacion', () => {
  it('should create an instance', () => {
    expect(new InformacionAutorizacion()).toBeTruthy();
  });
});
